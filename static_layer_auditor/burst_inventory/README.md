Directory containing caches of unique burst IDs obtained from CMR queries.

Each collection's cache files (`{COLLECTION}_{static|baseline}_cmr_cache.csv`) contain burst IDs found in CMR.
File modification time is used to determine incremental update dates. 